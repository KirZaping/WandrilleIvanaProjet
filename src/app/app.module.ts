import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';
// import { AppComponent } from './app.component'; // Supprimé

@NgModule({
  declarations: [
    // autres composants...
  ],
  imports: [
    BrowserModule,
    // autres modules...
  ],
  providers: [],
  // bootstrap: [AppComponent] // Supprimé
})
export class AppModule { } 